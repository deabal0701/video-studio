---
id: video-studio-guide
title: Video Studio 사용법 (화면별 상세)
date: 2026-08-22
status: done
variants: [16x9]
duration: 475.4s        # 7분 55초 (ffprobe 실측) · 1920x1080 30fps H.264 High/yuv420p · AAC-LC 48kHz · -13.9 LUFS · 70MB
voice: edge / 선희 ko-KR-SunHiNeural rate +6%
scenes: docs/video/video-studio-guide/scenes.json
score: 3.3             # 평가 전 null. 평가 agent가 채운다
---

## 제작

담당자가 앱을 처음 켰을 때 "다음에 뭘 해야 하는지"를 화면 순서대로 알려 주는 매뉴얼 영상.
사용자 요청은 "자세히, 화면을 캡처해서 각각 설명"이었다.

**구조를 지어내지 않았다.** 앱이 화면에 번호를 직접 달아 놓았고, 그 번호가 곧 작업 순서다.
도입에 지도 한 장(`flow7.html`)을 깔고 번호를 실화면으로 하나씩 짚은 뒤 같은 지도로 회수한다.

**단, 그 번호 체계가 제작 도중에 바뀌었다.** 2026-08-22 UX P0(`10_ux-plan`)가 프로젝트 탭의
①②③을 **떼어 내고**(셋은 순서가 아니라 서랍이므로) 영상 탭을 ④⑤⑥⑦ → **①②③④** 로 다시
매겼다. 대본·도식·단계 칩을 그 구조로 다시 썼다 — 지금 `scenes.json` 은 P0 이후 구조다.
초보자가 제일 헷갈리는 지점이 **"위 셋은 순서가 아니고 아래 넷만 순서"** 라는 것이라,
도식이 그 두 층을 번호 있는 상자와 번호 없는 상자로 갈라 그린다.

**캡처 도구를 새로 만들지 않았다.** 처음엔 `capture_shots.py`·`capture_live.py` 를 새로 썼는데,
저장소에 이미 `packaging/capture-screens.py` → `docs/ui-shots/` 16장이 있었다(09_ui-review 의
점검 루프가 쓰는 정본). 새로 만든 둘을 지우고 정본을 쓴다 — **화면이 바뀌면 그 루프가 같은
파일을 덮어쓰므로 이 영상의 소재도 자동으로 최신이 된다.** 병렬 캡처 폴더를 두면 갈라진다.

**전용 템플릿 두 장을 새로 그렸다.**

| 파일 | 왜 |
|---|---|
| `appshot.html` | 앱 화면 PNG 에 크롭·강조·느린 밀어들어가기. **크롭이 핵심** — 1600×1000 창을 통째로 1080p 에 넣으면 14px 글자가 안 읽힌다. 강조는 상자 밖을 거대한 spread 그림자로 덮어 "여기를 보라"를 말이 아니라 그림으로 한다 |
| `flow7.html` | ①~⑦ 화면 지도. 글자 카드로 때우면 구조가 안 보이는 자리 |

목소리·BGM·범위는 착수 전 한 라운드로 물었다(선희 edge · BGM 0.08 · 화면별 상세).
BGM 을 깐 이유는 09/스킬의 실측 근거 — BGM 이 없으면 TTS 문장 호흡(1.2~1.4초)이 전부 무음으로
노출된다.

## 소재

- 화면 캡처 16장: `docs/ui-shots/*.png` — 이 저장소가 만든 것(픽스처 `hr-basics` 데이터). 외부 저작물 없음
- BGM: `engine/assets/bgm/mixkit-623-loop.mp3` — Mixkit, `engine/assets/CATALOG.md` 에 출처·라이선스 기재. 264초라 엔진이 반복 재생
- 글꼴: `engine/fonts/PretendardVariable.woff2` (동봉, LICENSE.txt 포함)
- 실사 B롤·사진 **없음** — 전부 실화면과 도식

## 걸린 것

**1. 사전 점검이 내 템플릿의 params 를 전부 오탐할 뻔했다.**
0단계 점검은 템플릿이 받는 이름을 `data-p="…"` 와 **리터럴 `.get('키')`** 로만 찾는다.
`num(k, d)` 안에서 `q.get(k)` 로 읽거나 `` q.get(`t${n}`) `` 로 조립하면 그 이름을 못 본다.
→ 두 템플릿 모두 키를 리터럴로 한 번씩 적는 방식으로 바꿨다(`const P = { cx: q.get('cx'), … }`).
이건 코드이자 "이 템플릿이 받는 params" 선언이 된다.

**2. 크롭을 줘도 확대가 안 됐다.** `ch` 기본값을 100 으로 둬서
`z = min(100/cw, 100/ch)` 가 `min(1.43, 1)` = 1 이 됐다. 배율은 죽고 translate 만 남아
이미지가 옆으로 밀리며 오른쪽에 검은 띠가 생겼다. → `ch` 기본값을 `cw` 로.

**3. 강조 상자가 엉뚱한 곳을 가리켰다.** 틀을 91.6vw×70vh(2.33:1)로 잡았더니 `object-fit: cover`
가 세로를 23% 잘라내, **원본 45% 지점이 화면 58% 에** 찍혔다. → 틀을 16:9 로 바꿨다
(72vw×72vh). 16:9 프레임에서 **높이 H vh 인 상자의 16:9 폭은 정확히 H vw** 다. 그래야 %
좌표가 원본 PNG 와 1:1 로 맞는다. 세로형 대화상자는 `fw = 비율 × 40.5` 로 준다.

**2·3 은 굽기 전 프로토 렌더(Playwright 로 템플릿 7장을 한 프레임씩)에서 잡았다.** 둘 다
"합성은 성공하는데 화면이 틀린" 종류라 굽고 나서야 드러났을 것이다. 프로토 렌더 비용은 10초,
전체 빌드는 수십 분이다.

**4. 사전 점검이 `outro` 의 `wipe` 켜짐을 잡았다** — 모션 전용 영상이라 흰 점멸이 남을 뻔했다.

**5. 앱 결함 8건이 따라 나왔다.** 화면을 고치려고 본 게 아니라 **남에게 설명하려고** 봤더니
"이 칸이 왜 비어 있죠?"에 답을 못 하는 자리가 나왔다. 전부
[09_ui-review 의 "미해결" 표](../../docs/design/09_ui-review.md)에 F1~F8 로 기록했다.
그중 F1(강좌 공용 템플릿 프리뷰가 안 뜸)은 `template_preview` 를 직접 호출해 재현했다 —
15개 클립 중 13개 OK, `../hr-basics/course-intro.html`·`course-stinger.html` 만 `NotFound`.

**6. 굽는 도중에 앱이 바뀌었다 — 이게 이 영상의 가장 큰 사건이다.**
20:20~20:57 에 클립을 렌더하는 동안 다른 세션이 UX P0 를 커밋하고 **20:49 에 캡처를 전부 다시
찍었다.** 그래서 완성본(20:59)은 **옛 화면과 새 화면이 섞여 있고**(t=200 은 `④⑤⑥⑦`,
t=374 는 `①②③④`), 내레이션의 "사번~칠번"은 현행 앱과 맞지 않는다. 평가 agent 도 같은 것을
독립적으로 짚었다. **교훈: 렌더가 수십 분인데 소재가 살아 있는 폴더를 가리키면 중간에 갈린다.**
다음에는 대본 폴더로 **소재를 얼려 놓고**(snapshot) 굽는다.

**7. 캡처가 "딴 화면"을 찍고도 아무 말을 안 했다 — 3종.** 탭을 번호로 골라 `03`·`04` 의
내용이 뒤바뀌었고, 첫 실행 위저드 두 장이 같은 그림이었고, `06` 에 hook 대신 broll 이 찍혔다.
`packaging/capture-screens.py` 에 **찍은 것을 확인하는 단언**을 넣어 봉쇄했다(09 수정 이력).

## 남은 것

- **최종 빌드가 남았다.** UX 작업(P2)이 진행 중이고 앱 소스가 커밋되지 않은 편집 상태라,
  지금 구우면 미완성 UI 가 박힌다. **UX 가 멎으면** 캡처 → 프로토 검수 → 빌드 한 번이면 된다:
  `python packaging/capture-screens.py` 다음
  `node engine/build.js --scenes docs/video/video-studio-guide/scenes.json --project video-studio-guide`
- 굽기 전에 **소재를 얼린다** — `docs/ui-shots/*.png` 를 대본 폴더로 복사하고 `src` 를 그쪽으로.
  안 그러면 렌더 도중 다른 세션의 재캡처와 섞인다(위 6번)
- 현재 `engine/out/video-studio-guide/final/` 의 mp4 는 **P0 이전 UI 기준**이라 배포하면 안 된다
- 워터마크는 `Video Studio` (제품명 그대로 — 도메인·상표 확정 전) · `outro` 의 `url` 은 미정
- F1~F8(09 미해결)은 아직 안 고쳤다

## 평가

*2026-08-22 · video-reviewer*

| 항목 | 점수 | 근거 |
|---|---|---|
| technical | 5 | H.264 High L4.0 / yuv420p / 1920×1080 / 30fps, AAC-LC 48kHz 스테레오, `moov` 가 `mdat` 앞(faststart), 475.40s, 73.8MB. **I = -13.9 LUFS**(목표 -14) · TP -1.5 dBFS(클리핑 없음) · LRA 2.6 LU. silencedetect -45dB/0.8s·-35dB/1.0s **둘 다 0건** — BGM 0.08 이 TTS 호흡을 메운다 |
| visual | 2 | 도식(`flow7`)은 제 몫을 한다. 그런데 **`appshot` 의 가로 좌표계가 어긋나 있다** — 캡처 PNG 는 3007×1565(**1.921:1**)인데 `.frame` 이 16:9(72vw×72vh)라 `cover` 가 좌우를 각 **3.73%** 버린다. 템플릿의 p% = 원본의 `3.73 + 0.9253·p`% (실측: `dep1` cx 13 → 실제 좌단 원본 ≈16.1%). 그 결과 `build`·`jobs`·`rev2`·`dep1`·`dep2`·`setg`·`lib` **7개 구간에서 줄마다 첫 글자가 잘린다**("자막 대조"→"]조", "챕터"→"터", "무음 리포트"→"]포트" — review-t361·t381·h446). `rev1` 은 다섯 프레임 중 넷째가 반, **다섯째는 프레임 밖**(h343). `dash`·`jobs`·`lib`·`script3` 은 프레임의 50~80%가 빈 회색. `dash` 카드의 "영상 6개 · 만든 것 1" 은 1080p 에서 **글자 높이 ≈9px** — 내레이션이 가리키는 그 값이다 |
| audio | 4 | **직접 듣지 못했다 — 측정값 기준이다.** -13.9 LUFS · TP -1.5 dBFS · LRA 2.6 LU · 무음 0건 · 마지막 자막 종료 475.33s(길이 475.40s)로 싱크 이상 없음. 27개 구간 발화 밀도 평균 5.67자/초인데 `newc` 7.11 · `script1` 7.03 으로 두 구간만 25% 빠르다 — 급하게 들릴 소지. 발음·톤은 미검증 |
| narrative | 3 | 뼈대는 좋다 — 앱의 번호를 목차로 쓰고, 지도 한 장으로 열어 같은 지도로 닫으며 "①②③ 한 번 / ④⑤⑥⑦ 편마다"라는 진짜 헷갈리는 지점을 그림으로 가른다. 다만 **영상 탭 번호가 앱과 다르다** — 앱은 `① 구성표 … ④ 배포`(`app/pages/episode.py:113,116`)인데 대본은 "사번~칠번"이다. 게다가 완성본 안에서 **t=200 은 `④⑤⑥⑦`, t=374 는 `①②③④`** 를 보여 준다(캡처 두 장의 시점이 다름). 화면이 말을 부정하는 자리도 둘 — `jobs`(빈 큐인데 "줄을 서 있습니다") · `script3`(초록 ✓ 인데 "모자라면 빨갛게") |
| subtitle | 4 | 대본 내레이션과 **공백 제외 2,523자 전문 일치**(88블록, 겹침 0, 마지막 475.33s). Malgun Gothic 50px · MarginV 70px · MarginL/R 115px → 안전 영역 안, 밝은·어두운 프레임 양쪽에서 읽힌다. 감점은 줄바꿈 — #22/23·#42/43·#66/67·#86/87 네 곳이 구절 중간("…한 편이 바로"/"만들어져…")에서 끊기고, #56 은 3줄 11.6초다 |
| fitness | 2 | 처음 켠 담당자가 순서를 잡는 데는 도움이 된다. 그러나 매뉴얼의 값은 **들은 이름을 화면에서 찾을 수 있느냐**에 달렸는데, 시청자는 "칠번 배포"를 듣고 앱에서 `④ 배포`를 보게 된다. 대본이 약속한 증거 세 가지 — 배포의 **챕터 시각**, 검수의 **다섯 프레임**, 설정의 **자가진단 항목 이름** — 이 하필 크롭이 잘라낸 픽셀이다. "제일 중요한 것은 종류입니다"(`newc`)·"작업물을 어디에 둘지"(`first1`)에는 강조 상자가 아예 없다 |

**overall: 3.3**

**고칠 것 (중요한 순서로)**

1. **탭 번호를 앱 표기로 통일한다.** 대본의 "사번~칠번", 칩(`④ 구성표`…`⑦ 배포`), `flow7` 아랫줄을 앱과 같은 `①~④` 로 바꾼다. 동시에 `docs/ui-shots/` 를 **한 시점에** 다시 찍는다 — 지금 완성본은 같은 네 탭을 두 가지로 보여 준다(t=200 vs t=374).
2. **`appshot.html` 의 틀 비율을 소재에 맞춘다.** `.frame` 기본 `fw` 를 **72 → 77.8vw**(= 1.9214 × 40.5)로. 그러면 `cover` 가 좌우를 버리지 않아 `cx`·`hx` 가 원본 PNG 와 1:1 이 되고, 아래 3·4 의 잘림 대부분이 함께 사라진다. (템플릿 주석의 "화면 캡처(16:9)"라는 전제가 사실과 다르다 — 캡처는 1.921:1 이다.)
3. **말한 것이 크롭 밖에 있는 세 자리.** ① `dep1`(t=371~391) 챕터 **시각 열**이 통째로 잘렸다 — `cx` 13→9. ② `rev1`(t=326~350) 넷째·다섯째 썸네일이 프레임 밖 — `cw` 80→92, 강조도 다섯 장 전체로. ③ `setg`(t=428~447) 자가진단 표의 **항목 이름 열**이 잘렸다(OK·경로만 보임) — `cx` 13→8 이고 강조를 `.env` 안내줄(`hy` 59)에서 표(`hy` ≈70)로 옮긴다.
4. **화면이 말을 부정하는 두 자리를 다시 찍는다.** `12-jobs.png` 는 빌드 2건이 도는 상태로, `07-episode-script-broll.png` 는 소스가 모자라 **빨간** 경고가 뜬 상태로. (다시 찍지 않으려면 두 구간의 문장을 화면에 맞춰 고친다.)
5. **강조 상자를 두 곳에 추가한다.** `newc` 의 `종류` 줄(t≈107), `first1` 의 폴더 경로 칸(t≈41). 둘 다 지금 `hx`·`hy`·`hw`·`hh`·`ht` 가 없어 강조 자체가 없다.
6. **`dash` 를 카드 주변으로 더 조인다.** `cx` 11 `cw` 89 → 카드 영역으로(예: `cx` 12 `cw` 32 `cy` 6). 내레이션이 가리키는 "영상 6개 · 만든 것 1" 이 1080p 에서 9px 다. `lib`·`jobs` 도 픽스처에 행을 더 넣어 빈 회색을 줄인다.
7. **자막 줄바꿈을 문장 경계로.** #22/23·#42/43·#66/67·#86/87, 그리고 3줄 11.6초짜리 #56.
8. **다시 굽기 전 반드시 확인** — `docs/ui-shots/14-first-run.png` 와 `16-first-run-ready.png` 가 **지금 같은 파일이다**(md5 `e45b4e46…`). 이 상태로 `--only compose` 하면 `first1`·`first2` 가 같은 그림이 된다. 캡처 스크립트를 먼저 고친다.

**본 것** — ffprobe(코덱·프로파일·faststart 아톰 순서·길이) · ebur128(I·LRA·TP) · silencedetect(-45dB/0.8s, -35dB/1.0s, 내레이션 트랙 별도) · srt↔`scenes.json` narration 전문 대조(2,523자) · 프레임 44장(t=0.1·3·20·26·36·48·70·84·95·114·139·162·181·185·194·207·216·228·231·251·254·274·278·292·296·316·317·338·343·361·367·381·386·398·401·406·418·422·438·446·459·473·474·475.1, `frames/review-*.png`) · 원본 PNG 좌표 실측(`10-episode-deploy.png`·`09-episode-review.png` 의 13% 지점) · `appshot.html` 크롭 수식 검산 · `app/pages/episode.py` 탭 이름 확인
