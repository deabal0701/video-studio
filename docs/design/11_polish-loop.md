# 11 — 상용 품질 루프 (화면 하나씩, 무한 반복)

> **역할**: 이 루프를 도는 세션은 Video Studio 의 **기획자이자 화면 디자이너**다.
> 목표는 상용 서비스 수준 — 쉽게 쓰이고, 깔끔하고, 다음 행동이 항상 보이는 화면.
> (2026-08-22 사용자 지시: "무한하게 반복하여(24시간) 보완을 계속할 수 있는 loop")
>
> [09_ui-review](09_ui-review.md)가 1차 점검 대장이었다면, 이 문서는 **상시 순회 루프**다.
> 회차마다 화면 **하나만** 잡고 끝까지(수정·검증·푸시) 간다.

## 루프 프로토콜 (1회차 = 화면 1개)

```
1. 커서   아래 "순회 커서"의 다음 화면 하나를 잡는다
2. 캡처   conda run -n penv3.13-video python packaging\capture-screens.py
          + 필요하면 좁은 창(1180px)·상호작용 상태를 별도 스크립트로 재현
3. 검토   기획자의 눈으로 — 기본 기준 P1~P13 + 고도화 기준 P14~P25 를 대조하고,
          "회차별 추가 질문" 10개를 스스로 답한다. 수정 전 "AI 자기반문"을 거친다
4. 수정   발견한 것을 P0→P1→P2→P3 우선순위로 고친다 (기능 제거 금지 — 사용자 조건.
          P3 다듬기에 빠져 P1/P2 를 놓치지 않는다)
5. 검증   pytest 전체 + 재캡처 눈검수 (P24 는 실제 사용 흐름으로)
6. 기록   아래 회차 기록에 한 줄 + **최종 판정 4항** (아니오가 있으면 이유) + 커서 전진
7. 푸시   커밋 → origin/master (사용자 상시 지시)
8. 재시작 떠 있는 앱 창을 새 코드로 재시작
```

**추정 금지 — 반드시 캡처를 눈으로 본다. 캡처 자체도 의심한다** (09 캡처 함정 표).
큰 설계 변경(구조 개편·기능 추가)은 루프에서 하지 않는다 — 10_ux-plan 에 제안으로 적고
사용자 결정을 받는다. 루프는 **다듬기**다.

## 상용 기준 (P-체크 — 이 세션에서 사용자가 실제로 지적한 패턴의 일반화)

| # | 기준 | 원 지적 |
|---|---|---|
| P1 | **다음 행동이 화면에 보이는가** — 기억을 요구하지 않는다 | "어떻게 만들어야 할지 모르겠다" |
| P2 | **말이 담당자 언어인가** — 파일명·id·은어·개발 용어 금지 | "저장이 뭔가"·"이기고 있습니다"·"intro.html 을 어떻게 아나" |
| P3 | **간격이 숨을 쉬는가** — 폼 세로 ≥18px, 창 여백 ≥24px | "간격이 너무 작다" |
| P4 | **선택·상태가 눈에 보이는가** — checked·재생 중·진행 중 | "선택이 안 된다"·"중지를 어떻게"·"프로그레스바가" |
| P5 | **모든 문이 양방향인가** — 들어가면 나오는 길이 보인다 | "직접 쓰기 후 어떻게 돌아가나"·"①②③은 어디" |
| P6 | **좁은 창(1180px)에서도 안 깨지는가** — 가로 스크롤·잘림 금지 | "상단 틀이 안 맞는다" |
| P7 | **파괴적 동작이 보이면서 안전한가** — 삭제는 찾기 쉽고 확인을 거친다 | "삭제는 어디 있는가" |
| P8 | **재클릭·재진입이 동작하는가** — 이미 선택된 것을 눌러도 반응 | "대시보드 클릭해도 안 돌아간다" |
| P9 | **빈 상태가 말을 하는가** — 비어 있으면 무엇을 하면 되는지 | 09 G1 승계 |
| P10 | **입력이 쉬운 형태인가** — 자유 텍스트보다 콤보·프리셋·자동 생성 | "글자수 지정이 어렵다"·"id 자동으로" |
| P11 | **일관성** — 같은 개념은 같은 말·같은 위치·같은 모양 | "N강"↔"N편" 혼재 등 |
| P12 | **낭비 공간이 없는가** — 큰 빈 영역·중복 표시 | 09 G3·G5 승계 |
| P13 | **배치가 창 크기에 맞는가** — 내용이 좌상단에 몰리고 우·하가 크게 비면 위반. 콘텐츠 최대폭을 정하고 남는 폭은 의도적으로 배분한다 | "화면 사이즈에 비해 배치도 안 맞는다" (위저드 실측) |

**반복 확인 (52·53·54회차에서 연속 발견)** — 화면이 결과 라벨(저장됨·적용됨·경로 문제)을
들고 있으면 **대상이 바뀔 때 지우는지** 본다. 안 지우면 앞 프로젝트/앞 영상의 결과가
다음 화면에 그 대상의 것처럼 남는다 (대본 탭은 빨간 오류가 **다른 영상의 결함**으로 읽혔다).
같은 대상을 재로드할 때는 남겨야 한다 — 저장 직후 "저장됨 ✓"이 사라지면 안 되므로.

## 상용 UX 고도화 기준 (P14~P25 — 2026-08-23 사용자 지시로 확장)

P1~P13 이 **기능·레이아웃·일관성·사용 가능성**을 지킨다면, P14~P25 는 그 위에서
**인지부하·행동 흐름·피드백·복구성·시각적 완성도**를 본다.

| # | 기준 | 핵심 질문 |
|---|---|---|
| P14 | **3초 이해성** | 처음 본 담당자에게 3초만 보여주면 무슨 화면인지 말할 수 있는가 — 제목이 목적을 말하고, 가장 중요한 정보가 가장 먼저 보이며, 부가 정보가 핵심을 가리지 않는가 |
| P15 | **다음 행동의 명확성** (P1 강화) | Primary Action 이 **하나**인가, 주요 버튼이 서로 경쟁하지 않는가, 이름만 보고 결과를 예측할 수 있는가. 화면마다 "가장 먼저 해야 할 행동"을 한 문장으로 적어 본다 |
| P16 | **인지부하 최소화** | 다른 화면의 값을 기억하게 하거나, 같은 정보를 반복 입력하게 하거나, 파일명·id·경로를 직접 치게 하거나, 의미 모를 옵션을 늘어놓지 않는가 — "사용자가 기억하지 말고 화면이 보여준다" |
| P17 | **기본값 품질** | 아무것도 바꾸지 않고 실행해도 상용 서비스처럼 보이는 결과가 나오는가 (기본 선택·정렬·필터·크기·음성·버튼 상태) |
| P18 | **상태 전이 완성도** | Idle→Loading→Success→Error→Empty→Disabled 가 전부 정의돼 있는가 — 로딩 중 할 일, 성공 즉시 인지, 실패 원인 이해, 재시도 경로, 중복 클릭 차단 |
| P19 | **피드백 즉시성** | 클릭·저장·삭제·선택·재생·생성·이동 직후 상태 변화가 보이는가 — **"눌렀는데 아무 변화 없음" 금지** |
| P20 | **오류 예방** | 오류를 내고 설명하지 말고 애초에 못 내게 — 범위 강제·필수값 차단·형식 검증·삭제 확인·미저장 경고·중복 실행 방지 |
| P21 | **오류 복구성** | 실수한 사용자가 스스로 되돌릴 수 있는가 — 취소·재시도·원복. "처음부터 다시 해야 한다"면 개선 후보 |
| P22 | **시각적 위계** | 크기·위치·굵기·간격만 봐도 중요도(화면 목적→핵심 정보→주요 행동→보조→고급→위험)가 구분되는가 — 모든 버튼이 똑같이 강조되거나 위험 버튼이 일반처럼 보이면 위반 |
| P23 | **화면 밀도** (P3·P12·P13 보완) | 빈 공간이 **의미 있는 여백**인지 **콘텐츠 부족**인지 판단한다 — 관련 요소는 가깝게, 다른 그룹은 충분히 떨어지게, 카드가 불필요하게 쪼개지지 않게 |
| P24 | **실제 사용 시나리오** | 정적 캡처가 아니라 흐름으로 — ①정상(진입→입력→실행→확인) ②초보자(설명 없이 첫 행동→완료) ③실수(잘못 입력→오류→수정→재실행→완료) 3개를 실제로 돌린다 |
| P25 | **"싸구려 UI" 탐지** | 기능이 정상이어도 개발 중처럼 보이는 것 — 어색한 문구·과장된 버튼·다른 버튼 높이·미세한 여백 차이·정렬 어긋남·불필요한 테두리·과한 색·의미 없는 아이콘·기본 위젯 티·어색한 툴팁·레이아웃 점프·비어 보이는 카드·개발자용 티. "이 캡처를 고객에게 보여줘도 되는가?" |

### 회차별 추가 질문 (P-체크 대조 후 반드시 답한다)

1. 처음 보는 사람이 3초 안에 화면 목적을 이해하는가?
2. 첫 번째로 눌러야 할 버튼이 명확한가?
3. 사용자가 기억해야 하는 정보가 있는가?
4. 기본값만으로도 좋은 결과가 나오는가?
5. 클릭 → 처리 → 완료의 상태 변화가 명확한가?
6. 오류를 미리 예방하고 있는가?
7. 실수했을 때 쉽게 복구할 수 있는가?
8. 정보의 중요도가 시각적으로 구분되는가?
9. 초보자가 설명 없이 완료할 수 있는가?
10. 기능은 정상이어도 개발 중인 제품처럼 보이는 부분이 없는가?

### 수정 우선순위 — P0 → P1 → P2 → P3 순서로, P3 에 빠져 P1/P2 를 놓치지 않는다

| 급 | 뜻 | 예 |
|---|---|---|
| P0 | 사용 불가 | 동작 안 함·화면 깨짐·데이터 손실·잘못된 결과 |
| P1 | 사용자가 막힘 | 다음 행동을 모름·기능을 못 찾음·오류에서 복구 불가 |
| P2 | 사용성 저하 | 불필요한 입력·과도한 인지부하·일관성 문제·상태 피드백 부족 |
| P3 | 완성도 | 간격·정렬·문구·시각적 위계·미세 레이아웃·"싸구려 UI" 요소 |

### AI 자기반문 (수정 전에 반드시)

> "내가 지금 고치려는 것은 **실제 사용자의 문제**인가, 아니면 **개발자의 취향**인가?"

- **수정하지 않는다**: 개인적으로 안 예뻐 보일 뿐 · 사용성 개선 근거 없음 ·
  디자인 시스템(08_qt-style)과 충돌 · 큰 구조 변경 필요(→ 10_ux-plan 제안으로)
- **적극 수정한다**: 다음 행동을 모름 · 값을 기억해야 함 · 결과 확인이 어려움 ·
  실수하기 쉬움 · 복구가 어려움 · 같은 개념의 UI 가 화면마다 다름 ·
  화면이 개발자용 도구처럼 보임

### 최종 판정 (회차 기록에 남긴다 — "아니오"가 있으면 이유까지)

| 항목 | 질문 |
|---|---|
| 사용 가능성 | 처음 사용자도 설명 없이 사용할 수 있는가? |
| 완결성 | 정상·빈 상태·오류·로딩·성공 상태가 모두 존재하는가? |
| 일관성 | 다른 화면과 같은 개념·용어·동작 규칙을 사용하는가? |
| 상용성 | 실제 고객에게 그대로 보여줘도 되는 수준인가? |

## 순회 커서

순서: 01 대시보드 → 02 새 프로젝트 위저드 → 03 영상 목록(보드) → 04 프로젝트 설정 →
05 브랜드 킷 → 06 ② 대본(본체) → 07 ② 대본 B롤 → 08 ① 구성표 → 09 ③ 검수 →
10 ④ 배포 → 11 라이브러리 → 12 작업 큐 → 13 설정 → 14 첫 실행 위저드 →
15 AI 대화상자·진행 → 16 삭제/추가 등 소형 대화상자 → (처음으로)

**다음 볼 화면: 08 ① 구성표 (4바퀴째)**

## 회차 기록

| # | 일시 | 화면 | 발견 (P-체크) | 수정 |
|---|---|---|---|---|
| 1 | 2026-08-22 | 01 대시보드 | **P6 위반 — 카드 4열 고정이라 창이 좁으면 가로 스크롤바** (사용자 캡처에서도 하단에 보였다). P4 — 카드에 마우스를 올려도 반응 없음 | 열 수를 창 폭에서 계산해 재배치(reflow) — 가로 스크롤 소멸. 카드 hover 테두리 강조 |
| 2 | 2026-08-22 | 02 새 프로젝트 위저드 | **P13 — 접힌 상태에서 우·하가 크게 빔** (사용자 캡처 그대로). 미리보기 도입 후 좌열이 좁아져 프리셋 라벨 잘림(P6 회귀) · 안내문 말줄임 | 우측에 **실시간 타이틀 카드 미리보기**(이름·종류·색이 즉시 반영 — 빈 폭을 의미로 배분). 견본 아이콘 축소·고급 설정 문구 단축으로 잘림 해소, 안내문 줄바꿈 |
| 3 | 2026-08-22 | 03 영상 목록(보드) | **P11 — AI 초안 진입점 이중화**: 보드 버튼은 옛 입력창(근거·한 번에 없음), ② 대본은 완전판. P4/P11 — 보드 카드는 [열기] 버튼만 되고 카드 클릭은 안 됨(대시보드와 불일치) | [AI 초안] = 영상 만들고 → 영상 화면 → **완전판 AI 대화상자 자동 오픈** (open_episode_ai 신호 사슬, 스텁 실측: 생성·이동·탭②·대화상자 호출 1회). 만들어진 카드는 카드 전체 클릭으로 열림 + 손 커서 |
| 4 | 2026-08-22 | 04 프로젝트 설정 | **P10/P11 — 영상 길이가 자유 텍스트** ("5분 (약 1,850자 — 초당 6.4자)" 를 그대로 편집) — 위저드는 콤보+자동 계산인데 이 탭만 옛날식 | 길이 도우미를 core.kinds 로 승격(LENGTH_CHOICES·length_to_chars·length_value·length_display), 두 화면이 같은 콤보+자동 글자수 UX. 저장값은 예산 파서 호환("N자" 포함) 유지 |
| 5 | 2026-08-23 | 05 브랜드 킷 | **P2/P11 — 낡은 탭 번호** "① 설정" 2곳 (프로젝트 탭은 무번호가 된 지 오래). **P1 — "설정 탭에서 고칩니다"가 말뿐** (가는 길 없음) | "[설정] 탭" 표기로. **[설정 탭에서 색 바꾸기 →]** 버튼 신설 — 눌러서 바로 이동 (go_settings 신호) |
| 6 | 2026-08-23 | 06 ② 대본(본체) | **P2 — 화면 값 폼이 엔진 키를 그대로 노출** (`progress`·`wipe`·`subtitle` …). **P2 — "TTS 캐시 없음 — [TTS 실측 갱신]으로 만드세요"** — 은어 + 존재하지 않는 버튼명 참조. "▶ 캐시 재생"도 은어 | 키 한국어 라벨 사전을 core.kinds 로 승격(PARAM_LABELS·param_label — "제목 (title)" 식, 키는 엔진 계약이라 괄호 보조로 유지). 음성 문구를 "▶ 음성 듣기"·"음성 N초 준비됨 / 음성이 아직 없습니다 — [음성 다시 만들기]를 누르세요"로 — 실재하는 버튼만 가리킨다 |
| 7 | 2026-08-23 | 07 ② 대본 B롤 | **P2 — `videoStart`·`shade` 원시 키 라벨 + "src 기입" 개발 용어**. **P10 — 숫자가 자유 텍스트** (형식·범위를 모름). **P4 — B롤 영상이 재생 전엔 빈 화면**, 재생하면 멈출 길 없음. **P12 — 템플릿 프리뷰 전용 `0.0s`·`↻ 재생`이 B롤에서도 떠 있는 죽은 컨트롤** | 라벨을 param_label 로("시작 지점(초) (videoStart)"·"어둡기 (shade)"), 입력을 스핀박스로(0.5s·0.05 step, 범위 강제). 선택 즉시 시작 지점 프레임에서 멈춰 소재가 보인다 + 재생 버튼 토글(재생 중 "중지" — 음성과 같은 문법). B롤일 땐 템플릿 프리뷰 컨트롤 숨김. 추출 버튼 "B롤 프레임을 이미지(src)로 가져오기" |
| 8 | 2026-08-23 | 08 ① 구성표 | **P2 — 일관성 배너가 개발자 말**: "render.bgm 가 course.json 과 다르다" (필드 경로+파일명). **P2 — "클립 골격" 은어** (반영 버튼·완료 대화상자) | 렌더 키 한국어 사전 신설(core.kinds RENDER_LABELS·render_label — 배경음악·워터마크·자막 크기 등 11종), validate.consistency 가 "배경음악(bgm) 값이 프로젝트 설정과 다릅니다"로 말한다. 반영 버튼 "대본으로 반영 — 표에만 있는 구간을 대본에 추가", 완료문도 역할 라벨로 |
| 9 | 2026-08-23 | 09 ③ 검수 | **P6 — 1180px 에서 검수 프레임 5장이 가로로 넘쳐 "마지막" 프레임이 잘림** — 체크리스트가 "마지막 프레임을 보라"는데 스크롤 없이는 안 보인다. 세로 바까지 연쇄 등장. 재생 캡션이 파일명만 | 프레임을 로드 시 창 폭에 맞춰 축소(다섯 장이 항상 한눈에 — 셀 마진 0·세로 바 금지·높이 동조). 캡션 "완성본 <파일명>" |
| 10 | 2026-08-23 | 10 ④ 배포 | **P6 — 썸네일 후보 5장이 가로로 넘쳐 5번째가 잘림** (③ 검수와 동일 패턴 — 좁은 창에서 고를 후보를 다 못 본다) | 로드 시 창 폭 맞춤 축소(9회차와 같은 처방 — 세로 바 금지·높이 동조). 문안·챕터·저장 흐름은 담당자 언어로 이미 양호 |
| 11 | 2026-08-23 | 11 라이브러리 | **P8/P4 — 종류를 바꿔도 이전 선택의 흔적이 남음** (배경음악 목록인데 "대본 기입값: assets/broll/…"·미리듣기 활성). **P2 — 메모·출처 열에 출처 대장 마크다운 기호 날것**(`**`·백틱). "CATALOG.md" 단독 표기 3곳·"기입값" 용어 | 종류 전환 시 선택·캡션·미리듣기 상태 초기화. 표에 넣을 때 백틱·별표 제거(구성표와 같은 처방). "출처 대장(CATALOG.md)" 표기·"대본에 적는 경로"로 |
| 12 | 2026-08-23 | 12 작업 큐 | **P2 — "작업" 열이 원시 jobId** (facade 가 kind·only 를 안 내보내 화면이 말할 재료가 없었다). **P4 — 상태 색 사전이 정의만 되고 미사용** — 진행 주황·완료 초록이 안 보였고 실패만 빨강 | _job_view 에 kind·only 추가, 작업 열을 "빌드·TTS만·합성만·AI 작업"으로 (jobId 는 툴팁 강등). 상태 글자색을 사전대로 적용 |
| 13 | 2026-08-23 | 13 설정 | **P2/P10 — 제공자 콤보가 원시 값** "claude"·"openai" 를 그대로, 두 항목짜리가 키 칸 폭(720px) 전체. **P11 — 진단 상태 "OK"** 영문 혼재 | 표기 "Claude — 기본 (Anthropic)"·"OpenAI" (저장값은 원시 id 유지 — findData 기반이라 안전), 폭 260px. 진단 상태 "정상"으로. 키·점검·우선 적용 안내는 P0 때 이미 정비돼 양호 |
| 14 | 2026-08-23 | 14 첫 실행 위저드 | **P1 — [변경…]이 죽은 컨트롤**: 데이터 폴더를 골라도 아무 데도 저장 안 됨 — "바꾸면 다음 실행부터 적용됩니다"가 말뿐. P11 — 진단 "OK" 영문·일괄 표시 (설정 화면은 순차) | env 에 `.data-dir` 포인터 신설(환경변수 우선·깨지면 기본값 후퇴, 테스트 3건), 위저드 완료 시 포인터 기록+새 폴더에 완료 표시. 진단을 "정상"·순차 표시로 통일 |
| 15 | 2026-08-23 | 15 AI 대화상자·진행 | **P4 — 빈 글로 [시작]을 누르면 조용히 무시** (버튼은 활성처럼 파랗다). 파고 보니 **전역 결함**: QSS id 선택자(#primary)가 `:disabled` 일반 규칙을 이겨 **비활성 primary 가 어디서나 활성처럼 보였다** | [시작]을 입력이 있어야 활성화 + `#primary:disabled` 규칙 신설(전 화면 공통 수혜). 진행 모드(헤드 변신·흐르는 바·로그 한 줄)는 실측 양호 |
| 16 | 2026-08-23 | 16 소형 대화상자 | **P7 — 영상 하나를 지울 길이 없음** (facade delete_episode 가 화면 미배선 — 프로젝트 통삭제만 가능). **P2/P7 — 클립 삭제가 "s1a 클립을 삭제할까요?"** — id 날것·내레이션 소실 경고 없음·기본 버튼이 위험 쪽 | 보드 영상 카드에 삭제 버튼(대시보드와 같은 문법) + 확인 대화상자(완성본 경고·자리 복구 안내·기본 취소). 클립 삭제는 역할 라벨 + "쓴 내레이션 N자도 함께 지워집니다" + 기본 취소. 발견: 보드 레인 3개가 1180px 가로 넘침 → 2바퀴 후보 |
| 17 | 2026-08-23 | 01 대시보드 (2바퀴) + 03 보드 잔건 | 대시보드는 2바퀴 점검 깨끗 (reflow·hover·삭제·게이지 정상). 16회차에 적어 둔 **03 보드 P6 — 레인 3개 고정 가로 배치가 1180px 에서 완료 레인을 잘랐다** | 레인을 폭 기반 reflow 로 (대시보드 1회차와 같은 처방) — 좁으면 완료 레인이 아래로 쌓이고(2열·1열), 넓으면 3열 복원. 1180·1600 양 폭 캡처 실측 |
| 18 | 2026-08-23 | 02 새 프로젝트 위저드 (2바퀴) | **P6 — 고급 설정의 목소리 행 잘림**: 긴 라디오 라벨("선희 (여) · 말 속도 +8%") 2개 + 안내문이 한 행에 몰려 우측이 잘리고, 안내문이 행 높이에 눌려 겹침(QFormLayout 세로 팽창 함정) | 라디오는 이름만("선희 (여)"·상세는 툴팁), 안내문은 별도 폼 행 + 줄바꿈. 실시간 미리보기·프리셋 자동 선택·길이 계산은 2바퀴 점검 정상 |
| 19 | 2026-08-23 | 03 영상 목록(보드) (2바퀴) | 레인 reflow(17회차)·카드 클릭·영상 삭제(16회차) 유지 확인. **P2 — 완료 카드 캡션이 변형 코드 날것** ("16x9") | "완성본 16x9"로 — 여러 개면 "완성본 N개 · 16x9". 세 레인 모두 찬 상태 실측 |
| 20 | 2026-08-23 | 04 프로젝트 설정 (2바퀴) | 길이 콤보(4회차)·목소리 카탈로그·위험 구역 유지 확인 — 실질 위반 없음. P11 마이크로 2건: "BGM" 라벨(8회차 사전은 "배경음악")·"저장"이 대상을 안 말함 | "배경음악"·"설정 저장"으로 (10 #1 "키 저장" 문법) |
| 21 | 2026-08-23 | 05 브랜드 킷 (2바퀴 — P14~P25 첫 적용) | **P18/P20 — [되돌리기] 처리 중 상태 부재**: 버튼이 활성 그대로라 중복 클릭이 두 번 실행, "적용 중" 표시 없음. **P20/P11 — 덮어쓰기 확인의 기본 버튼이 위험 쪽**(Yes — 16회차 문법에서 이 화면만 빠짐) | 실행 중 버튼 잠금 + "적용 중…" → 완료/실패 시 복원. 확인을 warning + 기본 [취소]로. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예(수정 후) · 상용성 예 — P23 하단 여백은 보기 화면 역할상 수용(자기반문: 구조 변경 불요) |
| 22 | 2026-08-23 | 06 ② 대본(본체) (2바퀴) | **P18/P20 — [저장] 처리 중 잠금 없음**: 중복 클릭이 옛 etag 로 두 번째 저장을 보내 409 충돌 에러. **P18/P20 — 빌드·TTS 작업 중에도 클립 탭 [음성 다시 만들기]가 활성** — 재클릭이 중복 잡을 큐에 쌓았다 (_set_building 이 헤더 4버튼만 잠갔다) | 저장 중 버튼 잠금+"저장 중…"→완료/실패 복원. _set_building 잠금 대상에 클립 탭 tts_btn 추가. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| — | 2026-08-23 | **창 골격 (전 화면 공통)** — 사용자 지적, 순회 커서 밖 | **P25/P11 — 창의 윗변이 반쪽만 그어져 있었다.** 흰 제목줄(OS)과 흰 내비(`#ffffff`)가 맞닿아 좌측 200px 은 경계가 아예 없고, 내비가 끝나는 x=200 부터서야 `ffffff→f5f5f7` 로 선이 생겨 상단이 계단처럼 꺾여 보인다. 하단 QStatusBar 는 `border-top` 으로 이미 닫혀 있어 위아래가 비대칭이었다. 실측(dpr 1.75, 클라이언트 y=30): x=100 `ffffff→ffffff`(선 없음) / x≥205 `ffffff→f5f5f7` | 중앙 위젯을 세로 2단으로 바꿔 **전폭 1px `QFrame#topRule`**(색은 상태바와 같은 `SEPARATOR`) 을 제목줄 바로 아래 깔았다 — 재실측에서 x=100·199·205·300·640 전부 `d2d2d7`. 새 선에 첫 항목이 붙어 보여 내비 `padding-top` 8→12px. 대시보드·프로젝트·영상·설정 상단 재캡처 확인, pytest 147건 |
| — | 2026-08-23 | **창 좌상단 — 무엇을 채우나** (전 화면 공통) — 사용자 지적, 순회 커서 밖 | 윗변을 그은 뒤 남은 질문: 제목줄 아래 좌상단이 비어 보인다. **P22 — 시선이 가장 먼저 닿는 좌상단에 가장 약한 것(작은 회색 메뉴 글자)이 앉아 있었다.** **P11/P15 — [새 영상 만들기]가 "화면" 목록 안의 유일한 "행동"** 이라 화면 이동으로 오해되고 `blockSignals`·`QTimer` 특례 코드를 달고 있었다. (곁가지로 발견: 그 행이 끼어든 뒤 `capture-screens.py` 의 내비 행 번호가 한 칸씩 밀려 **라이브러리 자리에 대시보드가, 설정 자리에 작업 큐가** 찍히고 있었다 — 캡처는 오류를 안 낸다) | 좌측 열 머리에 **[+ 새 영상 만들기] 기본 버튼** 승격, 목록에는 화면만 남김(4항목). 워드마크 안(案)은 **캡처에서 기각** — 제목줄이 30px 위에서 같은 로고·같은 글자를 이미 그려 중복(P12). 대시보드 [+ 새 프로젝트]는 같은 대화상자라 보조 버튼으로 내림(P15 경쟁 해소). 캡처 스크립트는 행 번호 대신 **도착한 위젯으로 확인**하도록 고침. 기각한 안 6종은 [10_ux-plan](10_ux-plan.md) 추가 지적 11 |
| 23 | 2026-08-23 | 07 ② 대본 B롤 (2바퀴) | **P15/P24 초보자 — 피커(B롤·템플릿 공용)가 더블클릭으로만 선택** — [선택]/[취소] 버튼이 없어 표준 대화상자 문법과 다르고 더블클릭을 모르면 막힌다. **P9 — 소재가 없으면 빈 표만**. **P25 — 시작 지점이 소스 끝을 지나면 "쓸 수 있는 소스 -1.1s"** 음수 표기 | 피커에 [선택](행 골라야 활성)·[취소] 신설 — 더블클릭은 지름길로 유지. 빈 목록이면 표 대신 "라이브러리에서 받아 오세요" 안내(힌트도 숨김). 초과 시 "시작 지점이 소재 끝(N s)을 지났습니다 — 줄이세요" 전용 문구. **판정**: 사용 가능성 예(수정 후) · 완결성 예 · 일관성 예(수정 후) · 상용성 예 |
| 24 | 2026-08-23 | 08 ① 구성표 (2바퀴) | **P18/P20 — [구성표 저장]·[대본으로 반영] 처리 중 잠금 없음** (21~22회차와 같은 패턴): 저장 중복 클릭은 옛 etag 로 409, 반영은 중복 실행 + "반영 중" 피드백 부재 | 두 버튼 모두 잠금 + "저장 중…"/"반영 중…" → 완료/실패 시 복원. 예산 게이지·역할 라벨 표·원문 접힘은 2바퀴 점검 정상. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 25 | 2026-08-23 | 09 ③ 검수 (2바퀴) | **P0 경합 — 체크리스트를 연달아 빠르게 체크하면 체크가 사라질 수 있었다**: 각 토글이 낡은 스냅샷으로 저장을 쏘고 늦게 도착한 저장이 최신 것을 되덮음. P19 — 피드백이 저장 완료를 기다림 | 로컬을 정본으로 즉시 반영 + 저장 코얼레싱(한 번에 하나, 진행 중이면 끝난 뒤 최신 상태 재전송, 서버 응답으로 항목을 되덮지 않음). 5연속 토글 실측 — 즉시 "검수 완료" + 서버에 5항목 전부 저장. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 (수정 후) |
| 26 | 2026-08-23 | 10 ④ 배포 (2바퀴) | **P0/P21 — 문안을 고치고 다른 탭에 다녀오면 편집이 조용히 덮였다** (재현 실측). 더 파니 **저장한 youtube.md 도 재조회가 무시** — 파일이 SSOT 인데 화면이 매번 생성 초안으로 되돌아가 저장본 위에 초안을 재저장할 위험. P18 — 저장 잠금 없음 | dirty 추적(사용자 편집만) → 편집 중엔 재진입이 안 덮음. core deliverables.build 가 저장된 youtube.md 를 정본으로 읽음(saved 플래그·회귀 테스트, 구식 수기 픽스처는 제거). 저장 잠금+"저장 중…". **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 27 | 2026-08-23 | 11 라이브러리 (2바퀴) | **P20 — [받기]가 빈 URL·라이선스에도 활성** — 누른 뒤에야 "URL 을 넣으세요"로 설명 (예방이 원칙). 11회차 수정(종류 전환 초기화·마크다운 정리)은 유지 확인 | [받기]를 URL+라이선스가 있어야 활성 (15회차 [시작]과 같은 문법 — 비활성 primary 는 회색). 받기 진행 잠금·빈 상태·중지 토글은 기왕 정비돼 양호. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 28 | 2026-08-23 | 12 작업 큐 (2바퀴) | **P5/P21 — 실패한 작업에서 해당 영상으로 가는 길이 없음** (재시도하려면 어느 화면인지 기억해서 찾아가야 — P16 위반이기도). **P18/P19 — [선택한 작업 중단] 처리 중 잠금·즉시 피드백 없음** | 행 더블클릭 → 해당 영상 이동(open_episode 신호 + 안내 캡션 "실패한 빌드는 거기서 다시 시도하세요"). 중단은 잠금+"중단 요청 중…" → 완료/실패 시 복원. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 29 | 2026-08-23 | 13 설정 (2바퀴) | **P18 — [키 저장] 처리 중 잠금·피드백 없음** (점검·합성 테스트 버튼은 기왕 잠금 있음). 저장 후 상태 갱신(refresh — agent 상태·진단 재실행)은 이미 정비돼 있었다 | 잠금 + "저장 중…" → 완료/실패 시 복원. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 30 | 2026-08-23 | 14 첫 실행 위저드 (2바퀴) | **P21 — 키 저장이 비동기 + 실패 무시**: 위저드가 닫히는 시점의 백그라운드 저장이라 앱 종료와 경주가 되고, 실패해도 아무 말 없이 입력한 키가 사라졌다 | 동기 저장(파일 한 줄 — 실측 0.006s)으로 전환, 실패하면 "키 저장 실패 — [설정] 화면에서 다시 저장할 수 있습니다" 경고. 실패 경로는 강제 실패 주입으로 실측. 진단 순차·데이터 폴더 포인터(14회차)는 유지 확인. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 31 | 2026-08-23 | 15 AI 대화상자·진행 (2바퀴) | **P2/P11 — 헤더 상태 칩이 "running"·"failed" 영문 날것** (작업 큐는 12회차에 한국어인데 여기만). **P18 — AI 실패 시 사유가 로그에만 남고 시작 패널은 기본 문구로 복귀** — 게다가 늦게 도착한 비동기 콜백이 실패 안내를 되접는 경합까지 | 칩을 작업 큐 STATE_LABEL 재사용으로 한국어화. ai_progress_end(error) — 실패 사유를 시작 패널에 표시(재시작 버튼 포함), _ai_error 플래그로 늦은 sync 가 안내를 못 덮게. **판정**: 사용 가능성 예 · 완결성 예(수정 후 — Error 상태 신설) · 일관성 예(수정 후) · 상용성 예 |
| 32 | 2026-08-23 | 16 소형 대화상자 (2바퀴 — 2바퀴 완주) | **P19/P20 — [영상 추가]에서 빈 제목 OK 가 조용히 무시** (OK 상시 활성). **P20 — ElevenLabs 과금 확인의 기본 버튼이 실행 쪽** — 엔터 연타가 결제가 될 수 있었다 | 영상 추가 OK 를 제목이 있어야 활성(공백만도 차단 — QInputDialog 내부 버튼 배선). 과금 확인 기본 [취소]. 삭제류·피커·AI 대화상자는 1~2바퀴 수정 유지 확인. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 33 | 2026-08-23 | 01 대시보드 (3바퀴) | **P11/P15 — 빈 상태 안내가 낡은 길**: "오른쪽 위 [+ 새 프로젝트]" — [새 영상 만들기]가 좌측 열 머리 Primary 로 승격(지적 11)된 뒤 보조 버튼을 가리키고 있었다. **P25 — 안내가 상단 구석에 붙어** 첫 화면이 미완성처럼 보임. "BGM" 표기 잔재 | 안내를 "왼쪽 위 [새 영상 만들기]로 시작하세요"로(Primary 하나만 가리킴), 세로 중앙 배치(stretch — 보일 때만), "배경음악" 표기 통일. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예(수정 후) · 상용성 예 |
| 34 | 2026-08-23 | 02 새 프로젝트 위저드 (3바퀴) | **P18 — [만들기] 처리 중 잠금 없음**: 중복 클릭이면 자동 id 가 a·b… 로 물러나며 **프로젝트가 2개 생긴다** (실데이터 중복). **P20 — 빈 이름에도 [만들기] 활성** — 누른 뒤에야 오류 설명 | 이름이 있어야 활성 + 만드는 동안 잠금·"만드는 중…" → 실패 시 복원 (성공은 대화상자 닫힘). **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 35 | 2026-08-23 | 03 영상 목록(보드) (3바퀴) | **P18 — 영상 생성 3경로([영상 만들기]·[AI 초안]·[+ 영상 추가]) 전부 처리 중 잠금 없음** — 더블클릭이면 같은 n 에 생성이 두 번 나가 Conflict 오류 노출·이중 이동 | 공통 잠금(_begin/_end_create)으로 직렬화 — 더블클릭 재현 실측: 열림 1회·폴더 1개·잠금 해제 확인. **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 36 | 2026-08-23 | 04 프로젝트 설정 (3바퀴) | **P0 — 탭을 나갔다 오면 편집이 조용히 덮였다** (재현: 이름 고침 → 브랜드 킷 → 복귀 → 원복. 배포 탭 26회차와 같은 결함). **P18 — [설정 저장] 잠금 없음** (중복 클릭 → 옛 etag 409) | dirty 추적(제목·태그라인·대상·길이·제공자·목소리·배경음악·색 3종) — 편집 중엔 같은 프로젝트 재진입이 안 덮고, 저장/다른 프로젝트에서 해제. 저장 잠금+"저장 중…". **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 예 |
| 37 | 2026-08-23 | 05 브랜드 킷 (3바퀴) | **P2 — [되돌리기] 완료 문구가 파일명 날것** ("적용됨 ✓ — course-intro.html, course-stinger.html"). 21회차 잠금·기본 취소는 유지 확인(실측: 기본 버튼 "취소") | 표시명으로 — "적용됨 ✓ — 프로젝트 타이틀 카드 (course-intro.html) · 로고 전환 (course-stinger.html)" (kinds.screen_label 재사용). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 38 | 2026-08-23 | 06 ② 대본(본체) (3바퀴) | **P20 — 저장하지 않은 대본 변경 경고 부재**: 내레이션·화면 값·순서를 고치고 [← 프로젝트]로 나가면 조용히 사라짐 | dirty 추적(내레이션·화면 값·B롤 값·순서·추가·삭제 6지점, 저장/재로드 해제) + 이탈 확인 대화상자(기본 [취소]). 실측 6시나리오: 무편집 통과·편집 후 경고·취소 잔류·Yes 이탈·저장 후 통과. 내비 경유 이탈은 미보호 — 다음 바퀴 후보. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 39 | 2026-08-23 | 07 ② 대본 B롤 (3바퀴) | **P18 — [B롤 프레임을 이미지(src)로 가져오기]가 ffmpeg 추출(수 초) 동안 잠금 없음** — 더블클릭 중복 실행. 스핀박스·피커·토글(7·23회차)은 유지 확인. videoStart 상한을 소스 길이로 죄는 안은 기각(로드 시 기존 초과 데이터를 몰래 고친다 — 자기반문: 23회차 안내문이 안전망) | 잠금 + "가져오는 중…" → 완료/실패 복원. 실측: ffmpeg 실추출 완주(5s 지점 프레임 기입). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 40 | 2026-08-23 | 08 ① 구성표 (3바퀴) | **P20/P0 — 원문(plan.md) 편집이 무방비**: 편집 중 재로드가 덮고, [← 프로젝트] 이탈 확인이 대본만 봄 (38회차 가드의 사각) | 원문 dirty 추적(blockSignals 로 로드 구분) — 편집 중 재로드 생략, 이탈 확인이 대본·구성표를 함께 본다("구성표 원문에 저장하지 않은 변경…"). 저장 시 해제. 실측 6단계 통과. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 41 | 2026-08-23 | 09 ③ 검수 (3바퀴) | **P1 — "검수 완료 — ④ 배포 탭으로 넘어가세요"가 말뿐** (가는 버튼 없음 — 5회차 브랜드 킷과 같은 패턴). **P11 — 같은 일시정지 동작인데 영상 "일시정지"·B롤 "중지" 혼재** | 완료 시 [④ 배포로 →] 버튼 등장(클릭 → 탭 이동), 문구는 "검수 완료"로 간결히. B롤 토글 "일시정지"로 통일. 실측: 미완 숨김·완료 등장·클릭 이동. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예(수정 후) · 상용성 예 |
| 42 | 2026-08-23 | 10 ④ 배포 (3바퀴) | **P20 — [← 프로젝트] 이탈 확인이 배포 문안 편집을 안 봄** (38·40회차 가드의 마지막 사각 — 대본·구성표만 보호) | deploy_tab.has_unsaved 공개 + 이탈 확인이 대본·구성표·배포 문안 셋을 함께 본다 ("배포 문안에 저장하지 않은 변경…"). 실측: 경고·취소 잔류·저장 해제·이탈. 이로써 편집 4표면 전부 이중 보호(재로드 방지+이탈 확인) 완비. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 43 | 2026-08-23 | 11 라이브러리 (3바퀴) | **P12/P25 — 사진·글꼴 종류에서 재생 불가한 [▶ 미리듣기]가 회색으로 상주** (의미 없는 컨트롤). 소재 삭제 부재(P7·P21)는 파일+출처 대장+참조 보호가 얽혀 다듬기 범위 밖 | 미리듣기 버튼을 재생 가능한 종류(배경음악·B롤)에서만 표시 — 4종 실측. 삭제는 10_ux-plan 제안 12로 기록(사용자 결정 대기). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 44 | 2026-08-23 | 12 작업 큐 (3바퀴) | **P16 — 작업에 시각 정보가 없어** "이 실패가 언제 것인지"를 알 수 없었다 (세션 내 잡이 쌓이면 구분 불가) | Job 에 제출 시각(created_at) 추가 → _job_view 노출 → "시각" 열(HH:MM:SS) 신설. 12·28회차 수정(작업 이름·색·행 이동·중단 잠금)은 유지 확인. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 45 | 2026-08-23 | 13 설정 (3바퀴) | **P20 — 키 입력이 무방비** (편집 5번째 표면): 내비 재클릭이 refresh 로 편집을 덮고, 다른 화면으로 나가도 경고 없이 유실 | dirty 추적(키 필드·제공자·테스트 모드·OpenAI 모델) — 편집 중 재클릭은 값을 안 덮고(진단만 재실행), 내비 이탈 시 확인(기본 [취소] — 취소면 설정 잔류·하이라이트 복원). 실측 5단계 통과. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 46 | 2026-08-23 | 14 첫 실행 위저드 (3바퀴) | **P21 — 데이터 폴더 포인터 저장 실패가 조용한 후퇴** — 사용자는 "바꿨다"고 믿는데 기본값이 쓰인다 (30회차에 키 저장은 알림을 넣었는데 폴더가 빠짐) | 실패 시 "데이터 폴더 변경 실패 — 기본 위치를 그대로 사용합니다" 경고. 실측: 정상 경로(포인터 기록·완료 표시)·강제 실패(없는 드라이브) 경고. **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 47 | 2026-08-23 | 15 AI 대화상자·진행 (3바퀴) | **P21 — 잘못 추가한 근거 문서/폴더를 뺄 길이 없음** (취소하고 처음부터). **P16 — 진행 패널이 멈추는 길(헤더 [중단])을 안 알림** | [근거 비우기] 버튼(근거가 있을 때만 표시). 진행 기본 문구에 "위의 [중단]으로 멈출 수 있습니다" — 실재하는 버튼만 가리킨다(6회차 문법). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 48 | 2026-08-23 | 16 소형 대화상자 (3바퀴 — 3바퀴 완주) | **P18 — 삭제 3경로(프로젝트 삭제 2곳·영상 삭제)만 잠금 문법에서 빠짐** — 확인 직후 재클릭이 두 번째 삭제(NotFound 오류 노출)를 보낼 수 있었다. 검증 중 부작용: 삭제 프로브가 실 OUT_ROOT 산출물을 지워 BUILT 테스트 6건 skip → 픽스처 재현 빌드로 복원(148 전원 복귀) | 설정 위험 구역은 잠금+"삭제 중…"(실패 시 복원), 대시보드는 페이지 플래그, 영상 삭제는 생성·삭제 공통 잠금 재사용. 실측: 영상·프로젝트 실삭제 완주(잠금 왕복·폴더 소멸). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 49 | 2026-08-23 | 01 대시보드 (4바퀴) | **P12/P16 — 단발 종류(광고·홍보) 프로젝트 카드가 시리즈 문법으로 표시** — "영상 1개 · 만든 것 1"+진행바+"완료 0/1"은 1편짜리에 무의미하게 장황하고, 종류 구분도 안 보였다 (kind 는 이미 내려오는데) | 단발 카드는 한 줄 "종류 · 단발 영상 · 상태(제작 중/완성 — 완성은 초록)"로, 진행바는 시리즈만. 구식 단독 폴더는 상태 생략(배지 정보 없음). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예(수정 후) · 상용성 예 |
| 50 | 2026-08-23 | 02 새 프로젝트 위저드 (4바퀴) | **P20 — 폴더 이름이 자유 텍스트**: 규약(영문 소문자·숫자·붙임표)을 placeholder 로만 말하고, 한글·공백·대문자를 넣으면 만들기 후에야 오류 | 입력 validator(`[a-z0-9-]*`)로 어긋난 글자는 입력 자체가 안 된다. 실측 4케이스(허용 2·차단 2). pytest 153 (병렬 세션의 초안 결함 차단 테스트 +5 합류). **판정**: 사용 가능성 예 · 완결성 예 · 일관성 예 · 상용성 예 |
| 51 | 2026-08-23 | 03 영상 목록(보드) (4바퀴) | **P2/P11 — 보드 카드가 모든 종류를 "N강"이라 불렀다**: 홍보 프로젝트의 유일한 영상이 "1강 인사잇 서비스 소개"(실측). 같은 화면의 [+ 영상 추가]는 "N편 제목"이라 **한 화면 안에서 강/편이 갈렸다**. 파고 보니 화면만의 문제가 아니었다 — **`scaffold` 가 같은 말을 완성본에 찍고 있었다**: 전 단발 종류의 `outro.subtitle` = "제목 · 1강", 일반 종류는 타이틀 카드 `num` = "1강"(4종 생성 실측). 같은 영상을 보드는 "2강", 영상 화면 헤더는 "2편"이라 부르던 것까지 연쇄 | 세는 말을 `core.kinds` 로 승격(`unit`·`counter` — 강의만 "강", 나머지 "편"). 화면 4곳(보드 카드·[+ 영상 추가]·영상 헤더·브랜드 킷 견본)·스캐폴딩·유튜브 제목이 같은 함수를 쓴다. **셀 것이 하나뿐이면 번호를 안 붙인다**(49회차 단발 원칙) — 단발 `num` 은 빈 값, 엔진이 그 요소를 지운다(`_params.js`). 유튜브 제목도 단발은 `[프로젝트] N강 —` 문법을 버리고 제목만(안 그러면 "[인사잇 소개] — 인사잇 소개"로 겹친다). 실측 6항목(2편째를 더하면 번호 부활 포함) · pytest 190(+7) | **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예(수정 후) · 상용성 예 |
| 52 | 2026-08-23 | 04 프로젝트 설정 (4바퀴) | **P20 — 프로젝트 이름을 비우고 [설정 저장]이 그대로 통과**(실측 저장됨). 빈 이름이 저장되면 대시보드 카드·프로젝트 헤더·삭제 확인이 전부 이름 없이 뜨고 — `.get("title", id)` 는 키가 있으므로 폴더명으로 안 물러난다 — **`render.watermark.text` 까지 빈 문자열이 되어 완성본 워터마크가 사라진다**. 위저드 [만들기](34회차)는 막는데 이 탭만 빠졌다. **P16 — 화면이 프로젝트 종류를 말하지 않는다**: 위저드에서 고른 뒤 확인할 길이 없는데 골격·기본 길이·색·세는 말(51회차)이 전부 거기서 갈린다. **P19/P11 — 상태 글이 프로젝트를 넘어 남는다**(캡처에서 발견): hr-basics 화면에 "저장됨 — 일관성 어긋난 영상 1개: insait-promo-01". 검증 중 추가 발견: 저장 잠금이 **버튼에만** 걸려 버튼 밖 경로가 옛 etag 로 두 번째 저장을 보냈다 | 이름이 있어야 [설정 저장] 활성 + 툴팁(15·27·32·34회차와 같은 비활성 primary 문법), `_save` 자체도 빈 이름을 거절. 쓰기 경로인 `facade.put_course` 도 개설과 같은 규칙으로 막는다 (읽기는 계속 관대 — 파일이 SSOT). 폼에 읽기 전용 **[종류]** 행 신설("홍보 · 단발 영상 — 만들 때 정해집니다"). 프로젝트가 바뀔 때만 상태 글 초기화(저장 직후 "저장됨 ✓"은 남긴다). 저장 잠금을 `_saving` 플래그로 승격해 동작 자체가 재진입을 막는다(35회차 `_begin_create` 문법). 실측: 실수 흐름(비움→차단→복구→저장) · 저장 중 타이핑/재호출 · 상태 글 3단계 · pytest 191(+1) | **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예(수정 후) · 상용성 예 |
| 53 | 2026-08-23 | 05 브랜드 킷 (4바퀴) | **P17/P20 — 대비 검사가 프리셋에만 있었다.** `core/kinds.py` 는 종류 팔레트를 4.5:1 로 지키고 테스트까지 두는데, [설정] 탭은 **아무 색이나** 고르게 하면서 어디서도 재지 않았다. 밝은 배경을 고른 프로젝트 실측: 흰 제목 글자/배경 **1.0:1** · 브랜드/배경 1.3:1 — 프리뷰가 통째로 백지인데 화면은 아무 말도 안 했고, 그대로 영상이 나간다 (엔진 글자색은 `_base.css` 의 `--fg:#ffffff` 고정). **P19/P11 — 상태 글이 프로젝트를 넘어 이월**(52회차 설정 탭과 같은 결함이 이 화면에도): "적용됨 ✓ — 프로젝트 타이틀 카드"가 아무것도 적용 안 한 다른 프로젝트에 그대로 | 킷 폼에 **[대비] 행** 신설 — 제목 글자(흰색)·브랜드·보조를 배경과 재서 하나라도 4.5:1 미만이면 err 칩으로 "배경과 대비가 낮습니다 — … 영상에서 …가 묻힙니다 — [설정] 탭에서 색을 바꾸세요"(가는 버튼은 바로 옆에 이미 있다 — 5회차), 통과면 캡션으로 수치만. 계산은 기존 `kinds.contrast` 재사용(새 기준을 만들지 않는다). 프로젝트가 바뀔 때만 상태 글 초기화. 실측 2팔레트(19.4/6.9/11.5 통과 · 1.0/1.3/1.1 경고) · pytest 192(+1) | **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예(수정 후) · 상용성 예 — P23 하단 여백은 21회차 판단(보기 화면 역할) 유지 |
| 54 | 2026-08-23 | 06 ② 대본(본체) (4바퀴) | **P19/P11 — 상태 글 이월이 여기서는 오진이 된다**(52·53회차와 같은 패턴): 1강에서 저장한 "저장됨 — 경로 문제: broll: B롤 없음 …"이 **2강 화면에 빨간 오류로 그대로** 떠 2강의 결함처럼 읽혔다(실측). **P11 — 시작 패널과 예산 게이지가 서로 다른 말**: 패널은 "대본이 아직 비어 있습니다"인데 같은 화면 게이지가 "232자 (13%)" — 게이지만 골격 자리표시자("[…]")를 쓴 글로 셌다. **P6 — 클립 목록에 가로 스크롤바**(자리표시자 이름이 길다) + **가운데 편집 열이 1180px 에서 오른쪽이 잘림** — 측정하니 뷰포트 317px < 내용 최소 464px 인데 가로 스크롤은 금지 정책이라 그냥 사라졌다 | 자리표시자 규칙을 `spoken()` 한 함수로 모아 패널·게이지·길이 추정·클립별 글자수·삭제 경고가 같은 말을 쓴다(자리표시자 클립은 "골격 안내문입니다 — 아직 쓴 글로 세지 않습니다"로 왜 0자인지 설명). 목록은 말줄임+툴팁 전문(가로바 소멸). 가운데 열에 최소폭 490px 을 줘 부족분이 프리뷰 열로 가게 했다 — 1180·1440·1600 으로 요청해 실측(단, 영상 화면 최소폭이 1782px 이라 실제 창은 그보다 넓었다 — 55회차 정정). 영상이 바뀌면 저장 결과 초기화. 곁가지: 자리표시자를 안 세자 빈 대본이 "실측"으로 뒤집혀 "추정"으로 되돌림 | pytest 195(+3) · **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예(수정 후) · 상용성 예 |
| 55 | 2026-08-23 | 07 ② 대본 B롤 (4바퀴) | **P4/P19 — 미리듣기가 화면을 따라 나온다**: B롤을 재생한 채 다른 클립으로 옮기면 영상 위젯만 숨고 **재생은 계속됐다**(위치가 10.6s→12.2s 로 계속 늘어남 실측). 탭 전환·[← 프로젝트] 이탈도 마찬가지였고, 음성 미리듣기(AudioPreview)도 같은 구멍이었다. **P9/P18 — 소재가 하나도 없는 라이브러리를 "라이브러리 로딩 중…"으로 영원히 표시**했다 — 새 클론·새 설치가 정확히 그 상태다(미디어는 커밋되지 않는다). 조회가 실패해도 `fail=lambda e: None` 로 삼켜 같은 문구에 머물렀다. **P6(기록만) — 영상 화면 창 최소폭이 1782px**: 이 루프가 "좁은 창"이라 부르는 1180px 은 영상 화면에서 애초에 만들 수 없는 폭이었다 → **54회차의 "1180 실측" 표기를 정정한다** | `stop_media()` 한 곳으로 모아 클립 전환·탭 전환·[← 프로젝트] 세 경로에서 B롤·음성을 함께 멈춘다 (실측 4경로 전부 StoppedState). 라이브러리 상태를 **네 가지로 구분**: 불러오는 중 / 소재 0개("[라이브러리] 화면에서 받아 오세요") / 읽기 실패(사유 표시) / 길이 미상. 창 최소폭은 원인까지 측정(배너 501·제목 419·배지 218·버튼 182 — 세 열 합 689 는 무죄)했고 `setWordWrap` 만으로는 넓은 창에서 일곱 줄로 접혀(P25) 되돌렸다 — 헤더 2줄 재배치는 [10_ux-plan](10_ux-plan.md) **제안 13** 으로 넘긴다 | pytest 195 · **판정**: 사용 가능성 예 · 완결성 예(수정 후) · 일관성 예 · 상용성 **아니오** — 1180px 에서 열리지 않는다(제안 13 대기) |
| — | 2026-08-23 | 01 대시보드 — 49회차 보완 (사용자 지적, 순회 커서 밖) | **P3 — 단발 카드에서 진행바를 뺐더니(49회차) 같은 줄의 시리즈 카드와 높이가 어긋나** 격자가 들쭉날쭉해 보인다 | 단발 카드도 행 구성(부제·바·상태 줄)을 시리즈와 똑같이 — 바는 **회색**(완성=가득·제작 중=빈 트랙)이라 시리즈 진행바(파랑)와 안 헷갈리고, 상태(완성 초록/제작 중)는 셋째 줄로 이동. 구식 단독 폴더는 빈 캡션으로 줄만 세운다. 49회차의 "시리즈 문법 제거" 취지는 유지(개수·완료 수는 여전히 없음) |
