# 11 — 상용 품질 루프 (화면 하나씩, 무한 반복)

> **역할**: 이 루프를 도는 세션은 Video Studio 의 **기획자이자 화면 디자이너**다.
> 목표는 상용 서비스 수준 — 쉽게 쓰이고, 깔끔하고, 다음 행동이 항상 보이는 화면.
> (2026-08-22 사용자 지시: "무한하게 반복하여(24시간) 보완을 계속할 수 있는 loop")
>
> [09_ui-review](09_ui-review.md)가 1차 점검 대장이었다면, 이 문서는 **상시 순회 루프**다.
> 회차마다 화면 **하나만** 잡고 끝까지(수정·검증·푸시) 간다.

## 루프 프로토콜 (1회차 = 화면 1개)

```
1. 커서   아래 "순회 커서"의 다음 화면 하나를 잡는다
2. 캡처   conda run -n penv3.13-video python packaging\capture-screens.py
          + 필요하면 좁은 창(1180px)·상호작용 상태를 별도 스크립트로 재현
3. 검토   기획자의 눈으로 — 아래 상용 기준 P1~P12 를 하나씩 소리 내어 대조
4. 수정   발견한 것을 그 자리에서 고친다 (기능 제거 금지 — 사용자 조건)
5. 검증   pytest 전체 + 재캡처 눈검수
6. 기록   아래 회차 기록에 한 줄 + 커서 전진
7. 푸시   커밋 → origin/master (사용자 상시 지시)
8. 재시작 떠 있는 앱 창을 새 코드로 재시작
```

**추정 금지 — 반드시 캡처를 눈으로 본다. 캡처 자체도 의심한다** (09 캡처 함정 표).
큰 설계 변경(구조 개편·기능 추가)은 루프에서 하지 않는다 — 10_ux-plan 에 제안으로 적고
사용자 결정을 받는다. 루프는 **다듬기**다.

## 상용 기준 (P-체크 — 이 세션에서 사용자가 실제로 지적한 패턴의 일반화)

| # | 기준 | 원 지적 |
|---|---|---|
| P1 | **다음 행동이 화면에 보이는가** — 기억을 요구하지 않는다 | "어떻게 만들어야 할지 모르겠다" |
| P2 | **말이 담당자 언어인가** — 파일명·id·은어·개발 용어 금지 | "저장이 뭔가"·"이기고 있습니다"·"intro.html 을 어떻게 아나" |
| P3 | **간격이 숨을 쉬는가** — 폼 세로 ≥18px, 창 여백 ≥24px | "간격이 너무 작다" |
| P4 | **선택·상태가 눈에 보이는가** — checked·재생 중·진행 중 | "선택이 안 된다"·"중지를 어떻게"·"프로그레스바가" |
| P5 | **모든 문이 양방향인가** — 들어가면 나오는 길이 보인다 | "직접 쓰기 후 어떻게 돌아가나"·"①②③은 어디" |
| P6 | **좁은 창(1180px)에서도 안 깨지는가** — 가로 스크롤·잘림 금지 | "상단 틀이 안 맞는다" |
| P7 | **파괴적 동작이 보이면서 안전한가** — 삭제는 찾기 쉽고 확인을 거친다 | "삭제는 어디 있는가" |
| P8 | **재클릭·재진입이 동작하는가** — 이미 선택된 것을 눌러도 반응 | "대시보드 클릭해도 안 돌아간다" |
| P9 | **빈 상태가 말을 하는가** — 비어 있으면 무엇을 하면 되는지 | 09 G1 승계 |
| P10 | **입력이 쉬운 형태인가** — 자유 텍스트보다 콤보·프리셋·자동 생성 | "글자수 지정이 어렵다"·"id 자동으로" |
| P11 | **일관성** — 같은 개념은 같은 말·같은 위치·같은 모양 | "N강"↔"N편" 혼재 등 |
| P12 | **낭비 공간이 없는가** — 큰 빈 영역·중복 표시 | 09 G3·G5 승계 |
| P13 | **배치가 창 크기에 맞는가** — 내용이 좌상단에 몰리고 우·하가 크게 비면 위반. 콘텐츠 최대폭을 정하고 남는 폭은 의도적으로 배분한다 | "화면 사이즈에 비해 배치도 안 맞는다" (위저드 실측) |

## 순회 커서

순서: 01 대시보드 → 02 새 프로젝트 위저드 → 03 영상 목록(보드) → 04 프로젝트 설정 →
05 브랜드 킷 → 06 ② 대본(본체) → 07 ② 대본 B롤 → 08 ① 구성표 → 09 ③ 검수 →
10 ④ 배포 → 11 라이브러리 → 12 작업 큐 → 13 설정 → 14 첫 실행 위저드 →
15 AI 대화상자·진행 → 16 삭제/추가 등 소형 대화상자 → (처음으로)

**다음 볼 화면: 03 영상 목록(보드)**

## 회차 기록

| # | 일시 | 화면 | 발견 (P-체크) | 수정 |
|---|---|---|---|---|
| 1 | 2026-08-22 | 01 대시보드 | **P6 위반 — 카드 4열 고정이라 창이 좁으면 가로 스크롤바** (사용자 캡처에서도 하단에 보였다). P4 — 카드에 마우스를 올려도 반응 없음 | 열 수를 창 폭에서 계산해 재배치(reflow) — 가로 스크롤 소멸. 카드 hover 테두리 강조 |
| 2 | 2026-08-22 | 02 새 프로젝트 위저드 | **P13 — 접힌 상태에서 우·하가 크게 빔** (사용자 캡처 그대로). 미리보기 도입 후 좌열이 좁아져 프리셋 라벨 잘림(P6 회귀) · 안내문 말줄임 | 우측에 **실시간 타이틀 카드 미리보기**(이름·종류·색이 즉시 반영 — 빈 폭을 의미로 배분). 견본 아이콘 축소·고급 설정 문구 단축으로 잘림 해소, 안내문 줄바꿈 |
